apiVersion: v1
kind: Service
metadata:
  name: {{ include "kafkalistenerresearch.fullname" . }}
  labels:
    {{- include "kafkalistenerresearch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kafkalistenerresearch.selectorLabels" . | nindent 4 }}
