apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kafkalistenerresearch.fullname" . }}-test-connection"
  labels:
    {{- include "kafkalistenerresearch.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kafkalistenerresearch.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
